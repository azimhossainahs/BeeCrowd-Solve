#include <stdio.h>
    double cube(double R){
    R = R*R*R;
    return R;
    }
    int main(){
       double pi = 3.14159;
       double R;
       scanf("%lf",&R);
       double Rad = cube(R);
       printf("VOLUME = %.3lf\n",(4/3.0)*pi*Rad);

    return 0;
}
