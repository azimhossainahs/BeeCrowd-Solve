#include <stdio.h>
#include <math.h>
double sq(double x){
return x*x;
}
int main(){
  double x1,x2,y1,y2;
  scanf("%lf %lf",&x1, &y1);
  scanf("%lf %lf",&x2, &y2);
  double distance =sqrt((sq(x2-x1)+sq(y2-y1)));
  printf("%.4lf\n",distance);
  return 0;
}
