#include <stdio.h>
    int main(){
        int Number,hour;
        scanf("%d %d", &Number, &hour);
        float phour;
        scanf("%f",&phour);
       printf("NUMBER = %d\n",Number);
       printf("SALARY = U$ %.2f\n",hour*phour);
    return 0;
}
